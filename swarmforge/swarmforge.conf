# Host lieutenant. Default is grok with no extra args.
# Lieutenant grok --yolo
# Lieutenant claude --yolo
# Lieutenant cursor --yolo
# Cursor CLI uses the `agent` binary on PATH (https://cursor.com/docs/cli).